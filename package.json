{
  "name": "project",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev:frontend": "pnpm --filter src-frontend run dev",
    "build:frontend": "pnpm --filter src-frontend run build",
    "dev:server": "pnpm --filter src-server run dev",
    "build:server": "pnpm --filter src-server run bundle",
    "dev:all": "run-p dev:frontend dev:server",
    "dev": "tauri dev -- -- --dev",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "adm-zip": "^0.5.16",
    "npm-run-all": "^4.1.5",
    "tar": "^6.2.1"
  }
}
